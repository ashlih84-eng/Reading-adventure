import{describe,expect,it}from'vitest';import{SPRINKLE_MISSIONS}from'../lessons/data/sprinkleMeadows';import{readingLevel,validateCurriculum,validateLessonCurriculum}from'./readingLevels';import type{Lesson}from'../lessons/types';
describe('curriculum content validation',()=>{it('passes every rewritten Sprinkle Meadows passage',()=>expect(validateCurriculum(SPRINKLE_MISSIONS)).toEqual([]));it('declares controlled reading metadata on every mission',()=>{for(const lesson of SPRINKLE_MISSIONS){expect(lesson.readingLevel.targetGradeRange).toBe('K–2');expect(lesson.readingLevel.unfamiliarWords.length).toBeLessThanOrEqual(2);expect(lesson.readingLevel.targetSkill).toBe(lesson.supportingSkill)}});it('warns about academic words, excess new words, outside knowledge, and unsupported answers',()=>{const passage='The engineer used a variable.';const sample={...SPRINKLE_MISSIONS[0],id:'bad',passage,readingLevel:{...readingLevel(passage,'decoding',[{word:'one',explanation:'1'},{word:'two',explanation:'2'},{word:'three',explanation:'3'}]),unfamiliarWordsPreTaught:false},activities:[{id:'q',type:'main-idea',prompt:'Why?',targetSkill:'other',options:['Because'],answer:0,evidenceText:'not in text',requiresOutsideKnowledge:true},{id:'q2',type:'matching',prompt:'Match',targetSkill:'different'}]}as Lesson;expect(new Set(validateLessonCurriculum(sample).map(x=>x.code))).toEqual(expect.objectContaining(new Set(['too-many-unfamiliar-words','unprepared-vocabulary','academic-vocabulary','outside-knowledge','unsupported-answer','multiple-primary-skills'])))});});
