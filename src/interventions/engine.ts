import type{InterventionReason,InterventionRecord,InterventionSupport,InterventionTemplate,SkillEvidence}from'./types';
const mapping:Record<string,[InterventionReason,InterventionSupport]>= { 'vowel patterns':['vowel-patterns','word-pattern-mini-lesson'],'blends and digraphs':['blends-digraphs','word-pattern-mini-lesson'],'vocabulary':['context-clues','vocabulary-preview'],'sequencing':['sequencing','retelling'],'main idea':['main-idea','parallel-passage'],'oral reading':['comprehension-vs-fluency','listen-then-read']};
export function recommendInterventions(evidence:SkillEvidence[],existing:InterventionRecord[]=[]){const recent=evidence.filter(x=>x.score<.7);const groups=new Map<string,SkillEvidence[]>();for(const item of recent)groups.set(item.skill,[...(groups.get(item.skill)??[]),item]);const added:InterventionRecord[]=[];for(const[skill,rows]of groups){if(rows.length<2||existing.some(x=>x.id===`support-${skill}`&&!x.completedAt))continue;const[reason,support]=mapping[skill]??['unclear-words','parent-review'];added.push({id:`support-${skill}`,reason,support,createdAt:new Date().toISOString(),evidenceCount:rows.length})}const decoding=evidence.filter(x=>x.skill==='decoding').slice(-3),comprehension=evidence.filter(x=>x.skill==='comprehension').slice(-3);if(decoding.length>=2&&comprehension.length>=2&&decoding.every(x=>x.score>=.8)&&comprehension.filter(x=>x.score<.7).length>=2)added.push({id:'support-decoding-vs-comprehension',reason:'decoding-vs-comprehension',support:'shorter-passage',createdAt:new Date().toISOString(),evidenceCount:4});return[...existing,...added]}
export const INTERVENTION_TEMPLATES:InterventionTemplate[]=[{id:'vowel-team-boost',title:'Word Pattern Power-Up',reason:'vowel-patterns',support:'word-pattern-mini-lesson',childMessage:'Let’s use a helpful pattern to make this clue easier.',passage:'Rain paints a bright trail beside the train.'},{id:'main-idea-boost',title:'Small-Passage Spotlight',reason:'main-idea',support:'parallel-passage',childMessage:'A shorter clue can help us spot the big idea.',passage:'Bees move pollen between flowers. This helps many plants make seeds.'},{id:'fluent-listen-read',title:'Hear It, Then Lead It',reason:'comprehension-vs-fluency',support:'listen-then-read',childMessage:'Listen once, then make the passage your own.',passage:'The careful explorer marked each turn on the map.'}];
