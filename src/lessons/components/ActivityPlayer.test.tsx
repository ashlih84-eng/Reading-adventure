import{fireEvent,render,screen}from'@testing-library/react';import{describe,expect,it,vi}from'vitest';import{ActivityPlayer,scoreActivity}from'./ActivityPlayer';
describe('activity components',()=>{it('supports keyboard-operable ordering and structured scores',()=>{const done=vi.fn();render(<ActivityPlayer activity={{id:'o',type:'drag-order',prompt:'Order',correctOrder:['one','two'],options:['two','one']}} onProgress={()=>{}} onComplete={done}/>);fireEvent.click(screen.getByLabelText('Move two later'));fireEvent.click(screen.getByText('Check order'));expect(done.mock.calls[0][0]).toMatchObject({activityId:'o',score:1,correct:true})});it('supports accessible matching pairs',()=>{const done=vi.fn();render(<ActivityPlayer activity={{id:'m',type:'matching',prompt:'Match',pairs:[['re','again'],['un','not']]}} onProgress={()=>{}} onComplete={done}/>);fireEvent.change(screen.getByLabelText('Match for re'),{target:{value:'again'}});fireEvent.change(screen.getByLabelText('Match for un'),{target:{value:'not'}});fireEvent.click(screen.getByText('Check matches'));expect(done.mock.calls[0][0].correct).toBe(true)});it('scores written evidence',()=>expect(scoreActivity({id:'r',type:'retelling',prompt:'Retell',acceptedText:['seed','grow']},'The seed will grow.').score).toBe(1));it('renders complete vocabulary support',()=>{render(<ActivityPlayer activity={{id:'v',type:'vocabulary',prompt:'Learn',vocabulary:{word:'cautious',pronunciation:'KAW-shus',syllables:['cau','tious'],definition:'Careful to avoid danger.',example:'The cautious hiker checked the bridge.'}}} onProgress={()=>{}} onComplete={()=>{}}/>);expect(screen.getByText('cau · tious')).toBeVisible()})});
