import type{NpcDefinition}from'../types';
export interface NpcVoiceAdapter{speak(text:string,options:{rate:number;pitch:number;volume:number;onStart:()=>void;onEnd:()=>void}):void;stop():void;available():boolean}
export class BrowserNpcVoiceAdapter implements NpcVoiceAdapter{available(){return typeof window!=='undefined'&&'speechSynthesis'in window}stop(){if(this.available())speechSynthesis.cancel()}speak(text:string,o:{rate:number;pitch:number;volume:number;onStart:()=>void;onEnd:()=>void}){if(!this.available()){o.onEnd();return}this.stop();const utterance=new SpeechSynthesisUtterance(text);utterance.rate=o.rate;utterance.pitch=o.pitch;utterance.volume=o.volume;utterance.onstart=o.onStart;utterance.onend=o.onEnd;speechSynthesis.speak(utterance)}}
export class NpcVoiceService{constructor(private adapter:NpcVoiceAdapter=new BrowserNpcVoiceAdapter()){}speak(npc:NpcDefinition,text:string,volume:number,callbacks:{onStart:()=>void;onEnd:()=>void}){this.adapter.speak(text,{rate:npc.voice.rate,pitch:npc.voice.pitch,volume,onStart:callbacks.onStart,onEnd:callbacks.onEnd})}stop(){this.adapter.stop()}available(){return this.adapter.available()}}
export const npcVoiceService=new NpcVoiceService();
