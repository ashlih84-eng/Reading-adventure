import{describe,expect,it}from'vitest';import{newPlayer}from'../../profiles/types';import{progressReport}from'./progressReport';
describe('progress report',()=>{it('exports the selected child’s progress and disclaimer',()=>{const player={...newPlayer('Mina',{skinTone:'warm',hairStyle:'waves',hairColor:'#321',outfit:'#639',glasses:false}),completedLessons:['meadow-1'],readingMinutes:12};const report=progressReport(player);expect(report).toContain('Reader: Mina');expect(report).toContain('Lessons completed: 1');expect(report).toContain('not a formal diagnostic assessment')})});
