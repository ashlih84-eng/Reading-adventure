export type SkinTone='light'|'warm'|'deep';export type HairStyle='curls'|'waves'|'short';
export interface AvatarConfig{skinTone:SkinTone;hairStyle:HairStyle;hairColor:string;outfit:string;glasses:boolean}
import type{PlacementResult}from'../../placement/types';import type{MasteryRecord}from'../../lessons/types';import type{InterventionRecord}from'../../interventions/types';import type{PracticeRewardLedger}from'../../rewards/practiceRewards';
export interface ParentSettings{pin:string;microphoneEnabled:boolean;notes:string;masterMute:boolean;voiceVolume:number;musicVolume:number;effectsVolume:number}
export interface RestorationState{color:number;flowersBloomed:boolean;lightsOn:boolean;villainEffectRemoved:boolean;pathRestored:boolean;friendlyCharacters:boolean}
export interface AccessibilitySettings{reduceMotion:boolean;highContrast:boolean;textScale:number;lineSpacing:number;dyslexiaFont:boolean;calmReading:boolean}
export interface PlayerProfile{id:string;firstName:string;avatar:AvatarConfig;stars:number;inventory:string[];equippedItems:string[];selectedCompanion?:string;currentRegion:number;unlockedRegions:number[];completedLessons:string[];placementResult?:PlacementResult;placementHistory?:PlacementResult[];masteryByRegion?:Record<number,MasteryRecord>;restorationByRegion?:Record<number,RestorationState>;readingMinutes?:number;unclearWords?:Record<string,number>;parentSettings?:ParentSettings;interventionHistory?:InterventionRecord[];practiceRewards?:PracticeRewardLedger;activityProgress?:Record<string,unknown>;createdAt:string;lastPlayedAt:string;placementComplete:boolean;accessibility:AccessibilitySettings}
export const defaultParentSettings=():ParentSettings=>({pin:'2468',microphoneEnabled:true,notes:'',masterMute:false,voiceVolume:.8,musicVolume:.5,effectsVolume:.7});
export const defaultAccessibility=():AccessibilitySettings=>({reduceMotion:false,highContrast:false,textScale:1,lineSpacing:1.5,dyslexiaFont:false,calmReading:false});
export const newPlayer=(firstName:string,avatar:AvatarConfig):PlayerProfile=>({id:crypto.randomUUID(),firstName:firstName.trim(),avatar,stars:150,inventory:[],equippedItems:[],currentRegion:1,unlockedRegions:[1],completedLessons:[],placementHistory:[],masteryByRegion:{},restorationByRegion:{1:{color:0,flowersBloomed:false,lightsOn:false,villainEffectRemoved:false,pathRestored:false,friendlyCharacters:false}},readingMinutes:0,unclearWords:{},parentSettings:defaultParentSettings(),interventionHistory:[],practiceRewards:{awards:[]},activityProgress:{},createdAt:new Date().toISOString(),lastPlayedAt:new Date().toISOString(),placementComplete:false,accessibility:defaultAccessibility()});
