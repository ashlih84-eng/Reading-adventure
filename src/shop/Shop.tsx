import type{PlayerProfile}from'../profiles/types';import{SHOP_ITEMS}from'../rewards/catalog';import{ShopItemArt}from'../components/art/ShopItemArt';
export function Shop({player,onBuy,onEquip}:{player:PlayerProfile;onBuy:(id:string)=>void;onEquip:(id:string)=>void}){return <section className="shop-screen"><div className="screen-heading"><div><p className="eyebrow">Royal Star Shop</p><h1>Choose your adventure gear</h1></div><strong className="star-count" aria-label={`${player.stars} stars`}>Stars: {player.stars}</strong></div><div className="shop-grid">{SHOP_ITEMS.map(item=>{const owned=player.inventory.includes(item.id);const equipped=player.selectedCompanion===item.id||player.equippedItems.includes(item.id);return <article className="shop-card" key={item.id}><ShopItemArt item={item}/><h2>{item.name}</h2><p>{item.cost} stars</p>{owned?<button onClick={()=>onEquip(item.id)} disabled={equipped}>{equipped?'Equipped':'Equip'}</button>:<button onClick={()=>onBuy(item.id)} disabled={player.stars<item.cost}>{player.stars<item.cost?'Save more stars':'Buy'}</button>}</article>})}</div></section>}
