import{describe,expect,it}from'vitest';import{compareReading,normalizeSpeech}from'./compareReading';
describe('speech comparison',()=>{it('normalizes punctuation, capitalization, and contractions',()=>{expect(normalizeSpeech("I CAN’T, stop.")).toEqual(['i','cannot','stop'])});it('aligns exact sequences with confidence',()=>{const result=compareReading('the path is bright','the path is bright',4);expect(result.matchedPercentage).toBe(100);expect(result.confidence).toBe(1)});it('detects omissions',()=>{expect(compareReading('the bright path','the path').omissions).toBe(1)});it('detects substitutions',()=>{expect(compareReading('the bright path','the dark path').substitutions).toBe(1)});it('detects insertions and repetitions',()=>{const inserted=compareReading('the path','the long path');expect(inserted.insertions).toBe(1);const repeated=compareReading('the path','the the path');expect(repeated.repetitions+repeated.insertions).toBe(1)});it('returns unclear words and timing without using speed as the score',()=>{const result=compareReading('quiet meadow','quiet window',60);expect(result.possiblyUnclearWords).toContain('meadow');expect(result.wordsPerMinute).toBe(2);expect(result.confidence).toBeGreaterThan(0)})});
